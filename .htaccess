# Redirection vers public/ pour Symfony sur OVH mutualisé
# Ce fichier est déployé à la racine de new-portfolio/back/

RewriteEngine On
RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ public/$1 [L]
